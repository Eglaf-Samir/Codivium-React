# McqPayloadCallExample.v1.cs — Detail Reference

## What it is
Illustrative example showing how to call CodiviumMcq.BuildMcqPayload() with sample data.
For developer reference only — not called in production.

## Entry Point
```csharp
void McqPayloadCallExample.Run()
```

Constructs a minimal CodiviumRawData, calls BuildMcqPayload(), and prints the result.

## Used by
Development/onboarding reference only. Not wired to any production path.

## Limitations
- Example data only — not representative of real user data volumes
